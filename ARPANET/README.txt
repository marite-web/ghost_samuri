This are all experimental, demonstrations of a capability regarding UDP client server connection.

Ips on both scripts are currently set to the default LocalHost ip. Which is 127.0.0.1 with a 6024 port. 
The actual ip is to whatever you decide to port forward on, or whatever vps system is used. Port is the same.



The GUI is hot ass refer back to line one for further detail.
